## This is cpp terminal

To compile this code you need to run the

```bash
g++ main.cpp -o main -lreadline
```

This will compile the code and make sure you have added the API key in `.env` file to work with ai
